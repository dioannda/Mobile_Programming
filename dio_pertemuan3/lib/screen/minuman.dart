import 'package:flutter/material.dart';

class minuman extends StatelessWidget {
  const minuman({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Halaman Minuman"),
        backgroundColor: Colors.deepPurpleAccent[100],
      ),
    );
  }
}